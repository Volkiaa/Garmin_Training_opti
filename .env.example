# Garmin Connect Credentials
GARMIN_EMAIL=your_garmin_email@example.com
GARMIN_PASSWORD=your_garmin_password

# Token Storage Path (for MFA authentication persistence)
GARMIN_TOKEN_PATH=./.garmin_tokens

# Database
DATABASE_URL=postgresql+asyncpg://training_optimizer:training_optimizer_password@localhost:5432/training_optimizer
POSTGRES_USER=training_optimizer
POSTGRES_PASSWORD=training_optimizer_password
POSTGRES_DB=training_optimizer

# Backend
SYNC_SCHEDULE_ENABLED=true
SYNC_TIME=06:00

# Frontend
VITE_API_URL=http://localhost:8000/api/v1
